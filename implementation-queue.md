# Prompted — Implementation Queue

The authoritative issue graph for implementation work. Built from the deep-discovery package (`docs/discovery/`) and maintained in place by `work-on-issues` waves.

Every issue file lives in [`implementation/issues/`](implementation/issues/). Each issue is in exactly one state. The `Ready` frontier is recomputed whenever a state or dependency changes.

## States

`Ready` — all prerequisites `Done`; may enter a wave.
`In progress` — selected for the current wave or has unresolved work.
`Blocked` — cannot proceed because of a real prerequisite, external condition, or a required Planning/Design decision.
`Implemented` — implementation and wave checks passed; independent review has not yet cleared it.
`Done` — independent review found no blocking defect.

## Wave history

| Wave | Commit | Issue ids | Outcome |
|---|---|---|---|
| Wave 1 | `8253c51` | `GL-1`, `GL-2`, `GL-3` | GL-2, GL-3 `Done`; GL-1 `Implemented` (review-work). Review filed `REP-GL1-1` (host-leave orphan) and `REP-GL1-2` (client-navigation test gap), both `Ready`, blocking GL-1. |
| Wave 2 | `0964c00` | `REP-GL1-1`, `REP-GL1-2` | Both repair issues `Done` (closure review passed, review-work). Server rejects host `leave_group` so a group always keeps a host who is a current member; `leave-delete.spec.js` gained two browser click→navigate tests. Full chromium suite 76/76 green; oxlint 0 errors. `GL-1` now `Done`. |
| Wave 3 | `9695517` | `RT-1` | Timed submission & voting windows land; voting closes on its deadline (no early reveal); host sets each window with numeric value + unit with boundary clamping. Full chromium suite 77/77 green; oxlint 0 errors/23 baseline warnings. Closure review (change + adversarial) NON-BLOCKING; `RT-1` now `Done`. Non-blocking follow-up `REP-RT1-1` (minute-window round-trip) filed `Ready`. `RT-2` is unblocked. |
| Wave 4 | `b784369` | `RT-2`, `REP-RT1-1` | `RT-2` = per-round vote budget (default 10), "Share the wealth" spread/concentrate toggle, downvote spends only from budget, server-enforced `allowDownvotes` and downvote-lifetime-dock removal; `voteBudgetRemaining` contract. `REP-RT1-1` = minute-window round-trip fix in `windowLengths.js` + regression spec. Full chromium suite 84/84 green; oxlint 0 errors/23 baseline warnings. Closure review (change + adversarial) split: `REP-RT1-1` NON-BLOCKING → `Done`; `RT-2` BLOCKING (adversarial found DEF-1: zero-point votes bypass the budget and can force `public_override`) → stays `Implemented`, repair `REP-RT2-1` filed `Ready`. |
| Wave 5 | `1f25e44` | `REP-RT2-1` | Zero-point budget-bypass repair: `cast_vote` rejects upvotes with `points <= 0`; new `clampDownvoteCost` (min 1) applied to `downvoteCost` in settings and at cast time so a downvote always spends budget. Regression added to `vote-budget.spec.js`. Full chromium suite 88/88 green; oxlint 0 errors/23 baseline warnings. Closure review split: change-reviewer NON-BLOCKING, but adversarial found `REP-RT2-1-FRAC` — non-integer upvote points (no `Number.isInteger` check) re-open the unbounded vote-count `public_override` pump. `REP-RT2-1` stays `Implemented`, blocked by new repair `REP-RT2-2`. |
| Wave 6 | `14c6e14` | `REP-RT2-2` | Fractional-points repair: `cast_vote` now requires `Number.isInteger(points) && points >= 1`, rejecting fractions (`0.1`/`1e-9`) that could be spent as an unbounded vote-count `public_override` pump. Regression added to `vote-budget.spec.js`. Full chromium suite 90/90 green; oxlint 0 errors/23 baseline warnings. Closure review (change + adversarial) NON-BLOCKING → `REP-RT2-2` `Done`. This clears the vote-budget chain: `REP-RT2-1` and `RT-2` are now `Done`. |
| Wave 7 | `6c428d0` | `RT-3`, `HG-1` | Final wave: `RT-3` (Judge skip, fair no-repeat rotation across rounds) + `HG-1` (host-abandonment election with durable `lastSeenAt`, majority transfer, return-cancels). Both `Implemented`, awaiting closure review. Full chromium suite 100/100 green; oxlint 0 errors/23 baseline warnings. Closure review (change + adversarial) split: `HG-1` NON-BLOCKING → `Done`; `RT-3` BLOCKING (adversarial found `REP-RT3-1` — a Judge assigned via a skip or a host hand-pick is never recorded in the served set, so they can be re-drafted before an unserved member gets a turn) → stays `Implemented`, blocked by repair `REP-RT3-1`. |
| Wave 7 repair | `35e1d31` | `REP-RT3-1` | No-repeat-rotation repair: judges are now recorded as having served when they actually PLAY (`select_topic` picks a topic), not at the moment of assignment. So a skip-assigned / host-hand-picked Judge who plays is added to `judgeUseCase` and cannot be re-drafted before an unserved member gets a turn, while the full-skip-revert branch stays reachable. Full chromium suite 101/101 green; oxlint 0 errors/23 baseline warnings. Closure review (change + adversarial) NON-BLOCKING → `REP-RT3-1` `Done`. This unblocks and closes `RT-3`. |
| Wave 8 | `f97dbfc` | `RT-4` | Display-only bug: the Overview and Round tabs formatted `currentTheme.deadline` at three sites with no null guard, so during `topic_selection` (deadline is `null` by design until the Judge picks a topic) they rendered `new Date(null)` — an epoch date (observed "12/31/1969 at 7:00:00 PM") and a large negative "hours" value. Fix confined to `web-app/src/pages/GroupView.jsx`: shared `formatDeadline` / `formatHoursRemaining` helpers return a "Not set yet" placeholder when the deadline is absent or invalid; real deadlines render exactly as before. New UI regression `round-deadline-display.spec.js` (proven red against pre-fix code). oxlint 0 errors / 23 baseline warnings. Full suite green (2 unrelated WebKit `round-phases:211` YouTube-search flakes pass on isolated re-run). Initial review (change + adversarial) NON-BLOCKING → `RT-4` `Done`. Follow-up commit added `context.close()` cleanup to the new spec (adversarial non-blocking note). |
| Wave 9 | `20cf374` | `UI-1`, `UI-7` | Paired CSS fix (`GroupView.css`, `CreateGroup.css`) for the unscoped `.form-row input, .form-row select { flex: 1 }` leak. New spec `e2e/rules-layout.spec.js`. oxlint 0 errors / 23 baseline warnings; full suite 416/416 green. Initial review (change + adversarial) BLOCKING: both reviewers independently found the same defect — the `.rules-edit-form` grid's `minmax(300px, 1fr)` track was too narrow for the rigid 180px label + the new number-input rule (missing `min-width: 0`), so numeric rows overflowed their section at normal desktop widths. `UI-1`/`UI-7` stayed `Implemented`, blocked by repair `REP-UI7-1`. |
| Wave 9 repair | `10306fd` | `REP-UI7-1` | Grid track floor `300px`→`440px` (math tied to the real section/row padding, border, 180px label, gap) + `min-width: 0` safety net on the rules numeric input, matching the established `.window-control input[type=number]` pattern. `rules-layout.spec.js` strengthened with overflow/overlap/row-containment assertions at 1400px and 1920px — proven red against the pre-repair CSS (`scrollWidth 983 > clientWidth 964`), green after. oxlint 0/23; full suite 416/416 green (independently verified; a transient mass mobile-safari failure from two concurrent full-suite runs contending for one dev server was diagnosed and cleared on a clean solo re-run). Closure review (change + adversarial) NON-BLOCKING — both reviewers verified the 440px math against the real design tokens and the DOM/selector structure, confirmed the Timing input isn't double-constrained, and confirmed `CreateGroup.css` and the read-only `.rules-container` view are untouched. `REP-UI7-1` → `Done`; this closes `UI-1` and `UI-7`. |
| Wave 10 | `4e3a1b6` | `UI-3`, `UI-4`, `UI-5` | Three independent UI fixes implemented in parallel. `UI-3`: host setup button "Start Group" → "Start Round" with `setup-button primary`; e2e selectors moved to exact role locators. `UI-4`: during `topic_selection` (`currentTheme.title` null) the Overview card and Round tab read "Choosing This Round's Theme" and omit the empty title and the "This round's music challenge" boilerplate; stat labels stack above values (scoped to `.theme-stat`/`.round-stat`). New spec `current-theme-heading.spec.js`. `UI-5`: "Quick Theme Inspiration" removed from `/topics` with its CSS; side effect accepted — `/account` reuses those class names and had been styled by `ThemeIdeas.css` through global CSS load order, so it now renders with `Account.css` only (checked visually, clean). oxlint 0 errors / 23 baseline warnings; full suite 424/424 green. Initial review (change + adversarial) NON-BLOCKING for all three → `UI-3`, `UI-4`, `UI-5` `Done`. |
| Wave 11 | `f92d07c` | `EVT-1` | Server-side first-party event logging: `server/events.js` (append-only `events` table, fire-and-forget `logEvent`, HMAC-SHA256 pseudonymous actor ids, 12-month opportunistic prune, `EVENTS_DISABLED`), 19 events instrumented in `server.js` with no handler behaviour change and no PII in props. `invite_opened` deferred to `UI-2`; `game_completed` deferred (no game end exists). New `server/test/events.test.js` (6/6) and `e2e/event-log.spec.js`. Initial review split: change-reviewer NON-BLOCKING; adversarial BLOCKING on an acceptance-record gap only (the two skipped funnel events were not recorded in the issue) — resolved by the implementation notes in `EVT-1.md`, no code defect. Non-blocking review items folded in before commit (e2e race → condition wait, `ts` index for the prune, `settings_changed` only on real change, `history` guard, explicit test file). oxlint 0 errors / 23 baseline warnings; full suite 428/428 green. `EVT-1` → `Done`. |
| Wave 12 | `d94de54` | `UI-2` | Random 20-letter A-Z CSPRNG invite codes, `/join/<code>` link, group id split from invite code (UUID ids; join by code only; `findMemberGroup` gate on every group-scoped handler; host-only `reset_invite_code`; 10-failures/60s join throttle; legacy `GROUP…` groups joinable by their id until reset). `invite_opened` / `invite_code_reset` events. Server tests 11/11; oxlint 0/23; full suite 468/468. Initial review (change + adversarial) BLOCKING: both found that a legacy `?join=true` member is locked out after a host reset (the query is never stripped, so reloads re-join with the retired id). `UI-2` stays `Implemented`, blocked by repair `REP-UI2-1`. Adversarial audit otherwise clean: no bypass by id, no code leak, no index drift, no production test hook, no open redirect. |
| Wave 12 repair | `54f818e` | `REP-UI2-1` | `GroupView` loads with `get_group` first and only falls back to the legacy `?join=true` join on "Group not found"; the query is stripped after the group loads, so a member's reloads/reconnects never re-join with a retired id. Also fixed a load race (duplicate "Group not found" from the effect's second run; `group_updated` before load crashed) and un-stuck the Reset button on disconnect / modal close / unmount. New tests proven red against the pre-repair code. Server tests 11/11; oxlint 0/23; full suite 476/476. Closure review (change + adversarial) NON-BLOCKING → `REP-UI2-1` `Done`; this closes `UI-2`. |
| Wave 13 | `f35f428` | `OG-1`, `JR-1`, `GS-1`, `GT-1`, `NT-1`, `UX-1` | User-directed feature wave, worked interactively rather than as a planned wave, so the issue files were written from the implemented code afterwards. `OG-1` open groups (dashboard preview of 12, `/open-groups` search with 12/24/48 paging, view-only group page, live `open_groups_changed` with signature coalescing). `JR-1` join requests with a 3-decline limit, kick, ban/unban, no new member mid-round, host-only request/ban state. `GS-1` `totalRounds` enforced: a set finishes with final standings and the host starts a new one (closes the "never enforced" note for `totalRounds`; `maxPlayers` still open). `GT-1` host topic list when custom topics are off, with enough-topics guards, plus one play per topic per set. `NT-1` notifications: per-user rooms, a persisted per-player store capped at 100, 17 event types, bell panel of 3, `/notifications`, remove/clear. `UX-1` start-flow modals instead of a disabled Start, the players warning only at 1 player, a connection banner, and Create Group name validation. Server unit tests 11/11, web-app unit 4/4; oxlint 0 errors / 5 baseline warnings; full suite 484/484 across all five projects (one notification test raced with `JR-1`'s new join notice and was fixed to filter by type; `game-rules.spec.js` then passed 5 runs in a row). **No independent review has been run on this wave** — all six issues are `Implemented`, not `Done`. |
| Wave 14 | uncommitted | `DB-1` (phases 1-2), `DEP-1`, search fixes | `DB-1`: persistence moved off Render's ephemeral filesystem to Neon Postgres. Phase 1 split loading out of the `PersistentStore` constructor; phase 2 added a driver layer (`DATABASE_URL` -> Postgres, unset -> SQLite) with a per-key write queue, serialized DDL, TLS verification on, and a SIGTERM flush. Verified live: the tables exist in Neon and the user confirmed groups now survive repeated deploys. Phases 3 (`events.js`) and 4 (a dropped-cache regression spec) are outstanding. `DEP-1`: Dependabot plus a CI audit job that blocks on shipped high/critical advisories, reports dev-only ones, and keeps a CycloneDX SBOM; baseline was zero advisories in both packages. Search: a case-mismatch bug discarded the results of every query containing a capital letter (`cleanText` only trims, the client compared against a lowercased copy) — found from a user report, fixed, covered by a spec proven red against the old code; `MAX_RESULTS` 8 -> 25 (quota is per call, not per result); the `videoEmbeddable` filter dropped in favour of a permanent "Watch on YouTube" link; pasted YouTube links resolve via `videos.list` at 1 unit against search's 100. Hardening: `submit_video` no longer trusts the client's thumbnail (a crafted client could point every other player's browser at a tracking beacon — demonstrated by a test against the pre-fix server) and prefers server-verified title and channel. Server unit 24/24, web-app unit 4/4, oxlint 0 errors, full suite 500/500 on SQLite and 484/484 on Postgres. **No independent review.** |
| Wave 15 | uncommitted | `DB-1` (phases 3-4) | Worked by two agents in parallel, each in its own worktree, then merged and verified here. Phase 3: the event log moved onto the driver, so it lives wherever the rest of the data does. Both drivers gained `ensureEventSchema`/`insertEvent`/`pruneEvents`/`readEvents` — the read path is new and closes the standing `EVT-1` note that events could never be read back. Postgres needed a `seq BIGSERIAL` because every event in a round shares a timestamp and there is no rowid to break the tie. The HMAC secret moved from a `meta` row to `EVENTS_HASH_SECRET` (fatal if missing in production), which fixes actor ids rotating on every boot. `events.test.js` and `event-log.spec.js` are now driver-agnostic; `db.js` lost the SQLite handle it kept only for events. Phase 4: a `test_reload_stores` hook (guarded like the other test-only hooks) plus `persistence.spec.js` — a played group, topics, notifications, a profile and deletions, each with kept controls so "still gone" cannot pass against a store that reloaded empty. The agent proved it red by stubbing `driver.upsert`/`remove` to no-ops against a real server on a spare port. **The dual-driver strategy earned itself here:** the first Postgres run failed `game-rules` because the process-global reload clobbered another spec's in-flight write — invisible on SQLite, where the window is sub-millisecond, and fatal on Postgres, where it is a round trip. Fixed by making `persistence` its own Playwright project that every other project depends on, so it never runs alongside anything. Server unit 24/24, api 95/95 on Postgres, full suite green on SQLite. **No independent review.** |

## Done

| Id | Issue |
|---|---|
| `UI-2` | Random invite codes, `/join/<code>` link, group id split from invite code, membership gate, host code reset (Wave 12 + `REP-UI2-1`) |
| `REP-UI2-1` | Legacy `?join=true` member locked out after an invite-code reset (Wave 12 repair) |
| `EVT-1` | Server-side first-party event logging (Wave 11) |
| `UI-3`, `UI-4`, `UI-5` | Start Round button style/label, theme heading before a theme is picked + stat spacing, Quick Theme Inspiration removed from /topics (Wave 10) |
| `RT-4` | Round deadline UI showed `new Date(null)` (Unix epoch) before a topic is picked — null-guarded the three readouts (Wave 8) |
| `UI-1`, `UI-7` | Timing-unit dropdown label clipped + Rules edit-form layout (Wave 9 + `REP-UI7-1` repair) |
| `REP-UI7-1` | `.rules-edit-form` grid track too narrow, rows overflowed (Wave 9 repair) |
| `GL-1`, `GL-2`, `GL-3` | Leave/Delete, EditVideo/Overview, Dashboard dead controls (Waves 1-2) |
| `REP-GL1-1`, `REP-GL1-2` | GL-1 repairs (Wave 2) |
| `RT-1` | Two timed round windows (Wave 3) |
| `REP-RT1-1` | Minute-window round-trip fix (Wave 4) |
| `RT-2` | Per-round vote budget + "Share the wealth" + single downvote cost (Wave 4 + repairs) |
| `REP-RT2-1` | Zero-point budget bypass (Wave 5) |
| `REP-RT2-2` | Fractional-points vote-count pump (Wave 6) |
| `HG-1` | Host-abandonment election (Wave 7 closure) |
| `RT-3` | Judge can skip their turn, fair no-repeat rotation (Wave 7 + REP-RT3-1) |
| `REP-RT3-1` | No-repeat rotation records Judges by play, not assignment (Wave 7 repair) |

## Implemented (wave checks passed, independent review not yet run)

| Id | Issue |
|---|---|
| `OG-1` | Open Groups: find and join a group without an invite code (Wave 13) |
| `JR-1` | Join requests, 3-decline limit, kicks and bans (Wave 13) |
| `GS-1` | A game is a set of `totalRounds` rounds, and it ends (Wave 13) |
| `GT-1` | Host topic list when custom topics are off; one play per topic per set (Wave 13) |
| `NT-1` | Notifications: bell, `/notifications` page, per-player delivery (Wave 13) |
| `UX-1` | Start-flow modals, connection banner, wizard validation (Wave 13) |
| `DEP-1` | Dependency and supply-chain scanning: Dependabot, CI audit, SBOM (Wave 14) |

## Ready

| Id | Issue | Notes |
|---|---|---|

## In progress

| Id | Issue | Where it is |
|---|---|---|
| `DB-1` | Persistence is on an ephemeral filesystem: every group, topic, notification and profile is destroyed on each Render spin-down (15 idle minutes), restart and redeploy | All four phases done (async store lifecycle, still SQLite; new `persistence.test.js`). Phases 2-4 outstanding: Postgres driver behind the same interface, `events.js` port with a stable `EVENTS_HASH_SECRET`, dual-driver verification. Decisions settled: tests run against **both** drivers (SQLite locally, Postgres in CI), writes are fire-and-forget with a per-key queue. Target is Neon's free plan. See `implementation/issues/DB-1.md`. |

## Blocked

| Id | Issue | Blocked by |
|---|---|---|
| `UI-6` | Rename "topic" **and "theme"** → "prompt" (page, route, references) | Product decision — surface-only vs full rename (wire events + `topic_selection` phase + `topics` DB table + persisted keys; ~430 "topic" + ~280 "theme" sites + a `prompted.db` migration), and docs scope. "theme" → "prompt" is **confirmed in scope** (user); visible-string inventory in the issue. Sequence with `UI-4`/`UI-5`. |
| `PRIV-1` | Privacy policy page + consent management (CMP) | Product/legal decision — data-controller identity, jurisdictions, deletion-path scope, CMP choice. `Login.jsx:89` already claims a Privacy Policy that doesn't exist. Part A (policy page reflecting current data practices) is draftable now and pairs with `EVT-1`; Part B (CMP) is a prerequisite for GA4 / ads. Not legal advice — needs a lawyer's review. See `implementation/issues/PRIV-1.md`. |

The `GL-1/GL-2/GL-3` wave and its `REP-GL1-1` / `REP-GL1-2` repairs are `Done` (Waves 1-2). Wave 3 (`RT-1`) and Wave 4 (`RT-2` + `REP-RT1-1`) are `Done`. Wave 5 (`REP-RT2-1`) and Wave 6 (`REP-RT2-2`) are `Done`, clearing the vote-budget feature. Wave 7 (`RT-3` Judge skip + `HG-1` host election) and its `REP-RT3-1` repair are all `Done`. Wave 8 (`RT-4`, deadline-display null guard) is `Done` (initial review passed). Wave 9 (`UI-1` + `UI-7`) and its `REP-UI7-1` repair are all `Done`. All prior waves are `Done`. Wave 10 (`UI-3` + `UI-4` + `UI-5`) is `Done` (initial review passed). Wave 11 (`EVT-1`, event logging) is `Done`. Wave 12 (`UI-2`) and its `REP-UI2-1` repair are `Done`. Wave 13 (`OG-1`, `JR-1`, `GS-1`, `GT-1`, `NT-1`, `UX-1`) is `Implemented` and awaits an independent review. The triage batch has no `Ready` issues left; `In progress` — `DB-1` (data does not survive a spin-down; phase 1 of 4 landed). `Blocked` — `UI-6` (topic/theme→prompt rename, scope decision) and `PRIV-1` (privacy policy + CMP, product/legal decision). See also the monetization note below.

## Out of scope for now (tracked as notes, not ready issues)

These are real findings but are deliberately **not** ready implementation issues in this wave. Leave them as notes until a wave owns them:

- **`allowDownvotes` was not server-enforced** (finding `f.settings`): now owned by `RT-2` (per-round vote budget), which enforces `allowDownvotes` server-side. Note retained for history.
- **Voting had no deadline or host escape** (finding `f.deadline`): now owned by `RT-1` (two timed windows, voting closes on its deadline). The design/decision is settled (`docs/design/a1-voting-deadline-change-design.md`, `docs/design/a4-round-windows-change-design.md`). Note retained for history.
- **`maxPlayers` is displayed as a limit but never enforced** (finding `f.settings`): still open. The `totalRounds` half of this note is **resolved** — the user's product decision was that a group plays a fixed set of rounds and then ends, implemented as `GS-1` (Wave 13). `maxPlayers` needs its own decision: refuse the join, or keep it as guidance for the host?
- **Song preview, chat, voter identity, auto-start, skip-Judge settings are inert** (finding `f.settings`): removing, wiring, or completing them is a product decision, not a bug fix.
- **Theme suggestions (was "Quick Theme Inspiration")** (from `UI-5`): the hard-coded six-card preset grid is being removed from `/topics` in `UI-5`. The user wants to come back to the idea of offering theme suggestions properly (curated or generated, not a static grid). Needs product/design when picked up.
- **Monetization** (user direction, not an immediate need): the goal is to make money from the site. Options discussed, in rough order of fit:
  1. **Host / premium tiers** — larger groups, more rounds, private leagues, longer history, custom branding. Reuses the existing settings/tier surface. Best fit; monetizes host + social value without touching the play surface or the CSP.
  2. **Cosmetics / one-time unlocks / tip jar.**
  3. **Sponsored rounds** — a music brand sponsors a themed prompt.
  4. **Display ads (AdSense)** — wanted eventually, but a poor fit until real scale: SPA = few pageviews/session, games are low-CPM, ~30–40% ad-block in this audience, and ad scripts substantially loosen the helmet CSP. Prerequisites: a live domain, `PRIV-1` Part B (CMP), `EVT-1` (to know if engagement justifies it), AdSense approval. GA4 is **not** required for ads but helps optimize revenue and is needed to later run Google Ads to buy traffic. Place ads on a landing page and between-rounds/results screens only, never during active play; plan a relaxed CSP for marketing pages vs strict in-game. Needs a product decision to pick a direction and, for any of these, a payments integration.
- **Wave 10 review observations (not defects):** (a) the new "Choosing This Round's Theme" heading says "theme" while the adjacent badge and phase block say "topic" — owned by `UI-6`. (b) The client treats any truthy `currentTheme.title` as chosen; a whitespace-only title would show "Current Theme" over a blank line, but the server's `cleanText` trims and rejects empty text, so it is unreachable today. (c) `/account`'s quick-ideas grid no longer inherits `ThemeIdeas.css`'s responsive overrides (its own `auto-fill minmax(160px, 1fr)` grid still adapts); revisit with the theme-suggestions note. (d) `current-theme-heading.spec.js` checks stat stacking in `topic_selection` and `submission` only; voting/reveal rely on the rule being phase-independent CSS.
- **EVT-1 review observations (not defects):** events had no read path (**resolved** in `DB-1` phase 3: `driver.readEvents`); the e2e spec writes rows into the developer's real `server/prompted.db` like the rest of the suite and fails if the server runs with `EVENTS_DISABLED=1` or a reused dev server points at a different `PROMPTED_DB_PATH`; `member_joined` counts again after a leave + rejoin; the first event after each restart triggers a prune (indexed, cheap at current scale).
- **UI-2 / REP-UI2-1 review observations (not defects):** (a) `get_groups` returns raw group objects (incl. `hostNotices`) to non-host members — pre-existing, needs its own look. (b) The web app sets no `Referrer-Policy`; `/join/<CODE>` relies on the browser default (`strict-origin-when-cross-origin`) — add an explicit policy when the web app gets a production host config. (c) The token-driven socket swap (`SocketContext` `removeAllListeners` before `disconnect`) skips per-action `disconnect` handlers, so a reset sent in the ms before a swap can leave "Resetting…" until the modal is closed; a rare double throttle charge for a non-member on a second-boundary token swap. (d) Legacy `GROUP…` groups stay joinable by their guessable id until the host resets the code (accepted; throttle is the brake) — consider a one-time reset of legacy codes before launch. (e) `docs/discovery/` still says "the id doubles as the invite code". (f) The GroupView load effect's cleanup does a blanket `socket.off('error')`, which could drop another action's one-shot error listener if the effect re-runs mid-request (not reachable today).
- **27 native `alert()` popups** (`UX-1` review pass): `GroupView.jsx` 22, `CreateGroup.jsx` 2, `Dashboard.jsx` 2, `Account.jsx` 1. They block the page, can't be styled, read poorly on mobile, and are the last place in the app where an error appears outside the page. Replacing them with in-page messages is a mechanical but wide pass; it wants its own issue and its own test sweep.
- **Wave 13 was not independently reviewed**: the six issues are `Implemented`. The features were built and tested interactively with the user rather than through a planned wave, so no change-reviewer or adversarial pass has looked at them. Worth a review pass before launch, with attention to the moderation surface (`JR-1`) and to what a non-member can see (`OG-1`).
- **`docs/discovery/` is pre-`UI-2`**: it still describes the group id as doubling as the invite code, and predates Open Groups, join requests, notifications and set-based games. It is a historical discovery record, not a current description of the app; treat `implementation/issues/` and this queue as authoritative.
- **Load and smoke tooling exists now; four gaps remain** (test-layer review): `web-app/perf/loadtest.mjs` and `web-app/smoke/` cover concurrency and deployment. Baseline on a dev laptop, one Node server, SQLite: 400 sockets / 50 groups of 8, actor round trip p50 21ms, p95 52ms, max 268ms, 24MB pushed, **no** rate-limit refusals. The finding worth acting on is `notifyOpenGroupsChanged`: one socket received **450** app-wide `open_groups_changed` emits in 89 seconds, most of them for groups it has nothing to do with. Still uncovered: (a) neither the load nor the smoke tool runs in CI, so nothing catches a regression in them automatically (dependency scanning itself is now covered by `DEP-1`); (b) smoke can't play a game, because production refuses test-mode sign-in by design — a real post-deploy game check would need a dedicated seeded account; (c) the Google and YouTube seams are never exercised against the real services, only fixtures and test-mode, so a breaking change there would first appear in production; (d) acceptance criteria ("Done when" in each issue file) aren't mechanically linked to tests, so a criterion can go uncovered without anything failing.
- **Profile pictures, two separate features** (user interest, 2026-09-22): Google's `picture` URL is already verified and plumbed through (`auth.js:51` -> `server.js:1062`), it is just never stored on the profile or shown — avatars are emoji from `AVATAR_CHOICES`. Showing the Google photo therefore needs **no object storage at all**, only display work and a CSP `img-src` entry for `lh3.googleusercontent.com`. User-uploaded pictures are a much bigger issue: type/size validation, resizing, EXIF stripping (phone photos carry GPS), moderation (strangers meet through `OG-1`), deletion on account removal, and a storage bill. Worth splitting into two issues when picked up; the app would only ever store a URL, so the storage provider is not a lock-in.
- **`web-app/e2e/invite-code.spec.js` reads SQLite directly** (found during `DB-1` phase 3): it opens the database file with `better-sqlite3` to check the `groups` table, so it would fail against Postgres. It survives only because it is not in `API_SPECS`, and the CI Postgres job runs just the `api` project — so nothing ever runs it on the driver production uses. A real hole in the dual-driver coverage; the fix is to read through the store or move the check to an API assertion.
- **`withErrorHandling` only catches synchronous throws** (`server.js:114`, found during `DB-1` phase 4): an async handler's rejection escapes it and becomes an unhandled rejection instead of the intended "Something went wrong" reply. Nothing is broken today because every async handler guards its own awaits, but it is a trap for whoever adds the next one.
- **`createInviteIndex` cannot drop entries**: a reload can only add to it. Harmless today (codes and ids do not change across a reload), but the index cannot be rebuilt from scratch in a live process if anything ever needs that.
- **Session tokens cannot be revoked** (finding `f.norevoke`): a security change with its own review; needs scoping.
- **Account Settings toggles are cosmetic and Deactivate is a stub** (finding `f.ui-dead`): separate surface from the group wave; needs a product decision on what the toggles should do.
- **Downvote previously charged the voter as well as the target** (finding `f.downvote`): resolved as a desirable single-cost decision in `RT-2` (downvote spends only from the round budget; the lifetime score dock is removed). Note retained for history.
- **Judge loses the "Select as Winner" control after voting** (RT-1 + RT-2 closure observations): both closure reviews confirmed the gap persists — once the Judge spends their whole budget, `voteBudgetSpent` flips and hides the `isRoundLeader`-gated winner control (GroupView ~1111-1174), so in a 2-player round the Judge cannot reveal early via the UI after a full-budget vote. The server still honors a Judge's `czar_select_winner` and the voting deadline still closes the round, so no done condition is violated; it is a UX gap. Consistent with the no-early-reveal design (A1/A4). Not yet owned by a wave; re-assess when the voting panel is next reworked.
- **HG-1 departed-member ballots are not purged from an in-flight election** (closure observation): `group.election.votes` keeps a departed member's ballot, and `electionMajorityNeeded` recomputes over the live member roster, so a scaled-down electorate counts stale votes toward the threshold. Both closure reviewers traced all paths and found **no reachable outcome where a departed candidate wins hostship** (any recompute that could promote a departed candidate is blocked because the candidate id ceases to be valid after departure; a solo electorate has no valid non-self target), so the non-member-host invariant holds. It is a robustness/fragility note only, not a defect; add explicit purge on `leave_group` whenever the election feature is next reworked.
- **REP-RT3-1 regression test is a probabilistic pre-fix red** (closure observation): the new judge-skip test "a skip-assigned Judge who plays is excluded from a later skip re-pick" deterministically asserts a valid state on the fixed code, but against the pre-fix bug its round-2 re-pick draws uniformly from two eligible members, so `.toBe(remainingUnserved.id)` fails on roughly ~50% of runs rather than every run. It covers the exact gap as the done-condition requires and the fix is confirmed by both closure reviewers plus a full 101/101 green suite; the probabilistic nature is a test-strength note, not a blocker. If the rotation bookkeeping is next reworked, consider asserting the skip-pool state directly or looping to make it a strict red on any regression.
- **A never-serving offline member can stall the rotation reset** (REP-RT3-1 adversarial observation, pre-existing, out of scope): `recordJudgeServed` resets the cycle only when `currentServed.length >= group.players.length`. If a member is offline and never serves, the set can never reach that size, so the full-skip-reset/revert path may not fire while they remain. This is a design consequence of the "set size == players.length" reset combined with presence-independent rounds (a round can begin with few connected members), not something introduced by `RT-3`/`REP-RT3-1`; note if rotation semantics are next reviewed.